code

This folder contains main executable

third_party_lib

This folder contains building of external lib and installing in lib folder required dll's(windows) / .so's(ubuntu) / .Dylib's(macOs) and headers
